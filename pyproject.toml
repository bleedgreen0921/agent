[project]
name = "research-agent-platform"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["fastapi==0.115.12", "uvicorn==0.34.2", "psycopg[binary]==3.2.6", "alembic==1.15.2", "SQLAlchemy==2.0.40", "httpx==0.28.1"]

[project.optional-dependencies]
test = ["pytest==8.3.5"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
